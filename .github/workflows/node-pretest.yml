name: 'Tests: pretest/posttest'

on: [pull_request, push]

permissions:
  contents: read

jobs:
  tests:
    uses: ljharb/actions/.github/workflows/pretest.yml@main
    with:
      skip-engines: true # TODO: downgrade readable-stream, and either require 0.10 or make deps support 0.8
