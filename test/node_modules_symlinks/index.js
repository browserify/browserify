var local = require('lib/local');
t.equal(local(), 'boop', 'local module got local transform');

var real = require('realmodule');
t.equal(real(), 'robots', 'realmodule got real transform');

var real_no_xform = require('realmodule-no-xform');
t.equal(real_no_xform(), "beep", 'realmodule-no-xform did not get local transform');
